package commands

import "fmt"

func ConfigureCommand(args map[string]interface{}) (done chan bool) {
	return
}

func Configure(domain, accessToken string, bucketSize, refillRate int) {
	fmt.Println("Not yet implemnted. Sorry :(")
	return
}
